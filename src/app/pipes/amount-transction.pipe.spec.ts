import { AmountTransctionPipe } from './amount-transction.pipe';

describe('AmountTransctionPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountTransctionPipe();
    expect(pipe).toBeTruthy();
  });
});
